sudo: required
services:
    - docker
    
    
before_install:
    - docker build -t zmfrederick/docker-react -f Dockerfile.dev .

script:
    - docker run zmfrederick/docker-react npm test -- --coverage
    